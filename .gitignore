# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules
.kotlin

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
# Local Pi runtime state
.atl/
# Every APK, not one filename. APKs are generated artifacts and must never
# carry client-side credentials.
*.apk

# Generated by @nuxt/test-utils to track its own setup version
.nuxtrc

# scripts/extract-spec-pdf.py leaves these behind when imported
__pycache__/

# Convex codegen output. Regenerated from convex/schema.ts and the deployment,
# so it is a build artifact, not source.
convex/_generated/
