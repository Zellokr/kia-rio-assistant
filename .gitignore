# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules
.kotlin

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
# Local Pi runtime state
.atl/
# Every APK, not one filename. A field-test build carries a working bot
# token (see docs/FIELD_TEST_TELEGRAM.md), and committing one would be the
# exact leak the build gate exists to prevent.
*.apk

# Generated by @nuxt/test-utils to track its own setup version
.nuxtrc
